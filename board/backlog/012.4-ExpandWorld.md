# 012.4 — ExpandWorld + expansion metadata return/persist

Implement `ExpandWorld` to grow bounds, generate only new cells/chunks with continuous noise, and return/persist expansion metadata for scoped RegionalEngine fill.

**Parent:** `012-WorldEngine-Chunked-Map-Store`. **Depends on:** `012.3`.

## Acceptance criteria

- [ ] `ExpandWorld` generates only newly covered cells; prior cells unchanged
- [ ] Returned expansion metadata includes expansionId, addedBounds, previous/resulting bounds
- [ ] Unit tests cover expand → GetExpansion / ListExpansions consistency
- [ ] Regional fill can target the expansion without GetWorldWhole
