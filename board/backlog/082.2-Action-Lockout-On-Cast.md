# 082.2 — Apply Action lockout on successful cast

Successful casts apply lockout to the caster for the catalog-defined turn count on encounter/turn state.

**Parent:** `082-CombatEngine-Spell-Casting-And-Action-Lockout`. **Depends on:** `082.1`, `048-CombatEngine-Encounter-Lifecycle`.

## Acceptance criteria

- [ ] Successful cast applies Action lockout for catalog turn count
- [ ] Unit tests cover multi-turn lockout countdown behavior
- [ ] No mana pool introduced
