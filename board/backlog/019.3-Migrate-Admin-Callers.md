# 019.3 — Migrate Admin catalog / existing complete callers

Point ElectronAdmin (and any in-repo callers) at `completeText`; deprecate or remove chat-message public request if unused.

**Parent:** `019-LLMEngine-Text-Passthrough-API`. **Depends on:** `019.2`.

## Acceptance criteria

- [ ] Admin catalog lists/exercises `completeText` (and status/install)
- [ ] Chat-message-array public request is deprecated or removed with callers migrated
- [ ] Existing Admin smoke paths still work against the passthrough shape
