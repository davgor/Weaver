# 096.2 — set / get / clear / list location APIs

Publish CharacterEngine location façade + admin endpoints.

**Parent:** `096-CharacterEngine-Location-Ownership`. **Depends on:** `096.1`.

## Acceptance criteria

- [ ] `setCharacterLocation`, `getCharacterLocation`, `clearCharacterLocation` exported and unit-tested
- [ ] `listCharacterLocations(campaignId?)` or equivalent documented listing behavior (campaign index optional but tested if present)
- [ ] `setCharacterLocation` may stamp `updatedDay` from `getCampaignDay` when `campaignId` is supplied
- [ ] Admin `call` endpoints expose set/get/clear (and list if implemented)
- [ ] `characterEngine` README public API table updated in `096.4` or here
