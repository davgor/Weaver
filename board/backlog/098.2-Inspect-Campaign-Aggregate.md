# 098.2 — Readiness model + inspectCampaign aggregate

Define campaign readiness checklist and an inspect aggregate that walks peer public APIs for a `campaignId` + `dataRoot`.

**Parent:** `098-CampaignEngine-Full-Bootstrap-And-Admin-Inspect`. **Depends on:** `098.1`.

## Acceptance criteria

- [ ] `getCampaignReadiness` / `inspectCampaign` return structured JSON (counts + id lists + links) unit-tested with temp fixtures
- [ ] Covers at least world/regions/civs/NPCs/enemies when possible; missing peers report empty sections not crashes
- [ ] Admin `call('inspectCampaign', …)` endpoint works
- [ ] No generation in this ticket
