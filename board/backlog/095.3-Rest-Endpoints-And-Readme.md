# 095.3 — Rest endpoints + README

Wire the extended rest API through CharacterEngine admin endpoints and document the recovery contract.

**Parent:** `095-CharacterEngine-Rest-Recovery-Api`. **Depends on:** `095.2`.

## Acceptance criteria

- [ ] `call('longRest', { campaignId, characterIds? })` and `call('previewLongRest', …)` work via the published endpoint surface
- [ ] CharacterEngine README documents rest recovery (day + HP + conditions), clearable table, and that ActionEngine lockout clear is a caller concern
- [ ] Root README / package table needs no new package; CharacterEngine row may mention rest recovery if it still only says “day counter”
