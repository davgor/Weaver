# 019.5 — Consumer contract stubs / notes for NarrationEngine + DMEngine

Leave clear contract-test hooks/notes for when NarrationEngine and DMEngine call LLMEngine. Real `*.contract.test.ts` land when those packages actually call.

**Parent:** `019-LLMEngine-Text-Passthrough-API`. **Depends on:** `019.2`.

## Acceptance criteria

- [ ] Notes or stub paths document the expected consumer contract-test locations
- [ ] No fake Narration/DM invention inside LLMEngine tests
- [ ] Ticket does not implement Narration/DM call sites (those belong to later epics)
