# 098.7 — ElectronAdmin Campaign Explorer UI

Admin UI to generate a campaign and browse the inspect aggregate (tree or sections). Containers stay wiring-only; logic in tested helpers if any branching is needed.

**Parent:** `098-CampaignEngine-Full-Bootstrap-And-Admin-Inspect`. **Depends on:** `098.2`, `098.3`.

## Acceptance criteria

- [ ] Admin has a Campaign Explorer (or equivalent) distinct from raw endpoint JSON poking
- [ ] Operator can trigger `generateCampaign` and see readiness + artifact sections (world, regions, civs, NPCs, enemies, dungeons, quests, weather, locations)
- [ ] No game business rules in React/Electron wiring — calls CampaignEngine via existing dispatch patterns
- [ ] Basic unit tests for any extracted view-model/transform helpers
