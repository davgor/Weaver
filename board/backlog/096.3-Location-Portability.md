# 096.3 — Location portability slice

Persist character locations across campaign export/import.

**Parent:** `096-CharacterEngine-Location-Ownership`. **Depends on:** `096.2`, CharacterEngine portability from prior epics.

## Acceptance criteria

- [ ] `CharacterCampaignSlice` (or equivalent) includes location records
- [ ] Export/import round-trip restores `getCharacterLocation` for each character
- [ ] Unit/portability tests cover empty and non-empty location sets
- [ ] Schema/version bump documented if the slice format is versioned
