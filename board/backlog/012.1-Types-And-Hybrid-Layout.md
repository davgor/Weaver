# 012.1 — Types + hybrid layout (chunk format + SQLite metadata + expansions schema)

Define the WorldEngine storage layout and TypeScript types for cells, chunks, world metadata, and expansion records. No generation or public Create/Expand APIs yet.

**Parent:** `012-WorldEngine-Chunked-Map-Store`.

## Acceptance criteria

- [ ] Cell / chunk / world-meta / expansion record types are exported and unit-tested for shape invariants
- [ ] Hybrid layout documented in code: packed terrain chunks + SQLite metadata/overlays/expansions
- [ ] Expansion record fields cover expansionId, sequence, added/previous/resulting bounds
- [ ] No CreateWorld/ExpandWorld implementation in this ticket (types + schema only)
