# 102.1 — Campaign-gen quest seed stage

Add a post-peers stage (or post-`persist` hook) that seeds world quests.

**Parent:** `102-DMEngine-Campaign-Gen-Quest-Seed-Stage`. **Depends on:** `097`.

## Acceptance criteria

- [ ] `runCampaignGeneration` (or documented adjacent hook invoked by the pipeline) seeds world quests for the campaign
- [ ] Id pools come from peer engines already populated by earlier stages
- [ ] Contract test exercises real `@weaver/quest-engine` seed API
