# 082.4 — Action definition model (unified)

Action definitions reference range + effects + turn cost; flavor tags are non-mechanical.

**Parent:** `082-ActionEngine-Ability-Effect-And-Range-Model`. **Depends on:** `082.2`, `082.3`.

## Acceptance criteria

- [ ] Action CRUD/query by `actionId` with range, effects[], cost, flavor tags
- [ ] Unit test: two actions sharing an effect id remain distinct actions
- [ ] Unit test: changing only flavor tags does not change mechanical equality helpers used by validation
