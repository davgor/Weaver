# 098.6 — Bootstrap: WeatherEngine initial field / readiness

Apply or stage initial weather so inspect shows climate state for starting bounds/day 0.

**Parent:** `098-CampaignEngine-Full-Bootstrap-And-Admin-Inspect`. **Depends on:** `098.3`, `094`.

## Acceptance criteria

- [ ] Generation applies initial weather field (or equivalent readiness) via WeatherEngine public API
- [ ] `inspectCampaign` summarizes weather overlays / sample cells
- [ ] Contract test against real WeatherEngine + WorldEngine
