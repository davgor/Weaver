# 098.3 — Host/migrate generateCampaign pipeline + Admin endpoint

CampaignEngine becomes the entrypoint for full campaign generation (wrap DMEngine `runCampaignGeneration` first if needed, with a clear migration path to own the stage graph).

**Parent:** `098-CampaignEngine-Full-Bootstrap-And-Admin-Inspect`. **Depends on:** `098.2`, `052`.

## Acceptance criteria

- [ ] `generateCampaign` creates/opens campaign SQLite and runs the content pipeline against real peer APIs (fixtures for Narration)
- [ ] Admin endpoint `generateCampaign` callable from ElectronAdmin catalog
- [ ] Consumer contract tests: CampaignEngine → DM persistence + at least World/Regional/Civ or documented wrap boundary
- [ ] Document interim dual-path if AITTRPG still calls DMEngine directly
