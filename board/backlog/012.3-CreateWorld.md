# 012.3 — CreateWorld (incl. expansion 0 metadata)

Implement `CreateWorld`: create world id + store, generate initial rectangle via Perlin, persist chunks + metadata, and record expansion 0 with its AABB.

**Parent:** `012-WorldEngine-Chunked-Map-Store`. **Depends on:** `012.1`, `012.2`.

## Acceptance criteria

- [ ] `CreateWorld` persists chunks + SQLite meta and returns a world id
- [ ] Expansion 0 is recorded with addedBounds matching the initial rectangle
- [ ] Unit tests cover create + reload from disk for the same world id
- [ ] Exposed via typed API and admin-callable endpoint pattern
