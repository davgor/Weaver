# 081.4 — Electron/DM call-path wiring notes + open/migrate tests

Ensure renderer never talks SQL; DMEngine (or the persistence module owned here) is the call path.

**Parent:** `081-DMEngine-Campaign-Persistence-And-Migrations`. **Depends on:** `081.2`, `081.3`.

## Acceptance criteria

- [ ] Call-path documented: Electron → DMEngine/persistence API → SQLite
- [ ] Unit tests cover open/migrate gate without booting Electron
- [ ] No raw SQL IPC channel introduced
