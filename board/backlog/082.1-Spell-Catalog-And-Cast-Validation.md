# 082.1 — Spell catalog cost types + cast request validation

Catalog-driven spell cost types and reject-unknown-id validation. No lockout application yet.

**Parent:** `082-CombatEngine-Spell-Casting-And-Action-Lockout`. **Depends on:** `048` encounter lifecycle (for later lockout) can land in parallel after types; this ticket is catalog/validation only.

## Acceptance criteria

- [ ] Spell catalog entries expose Action-turn cost (not mana)
- [ ] Cast validation rejects unknown spell ids (unit-tested)
- [ ] LLM-proposed cost fields are not part of the accepted API
