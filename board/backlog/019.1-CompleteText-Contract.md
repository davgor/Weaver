# 019.1 — Types + completeText contract

Define the published passthrough contract: prompt + optional context in, raw text out. Unit tests with an injectable runtime fake — no real model required.

**Parent:** `019-LLMEngine-Text-Passthrough-API`.

## Acceptance criteria

- [ ] `completeText({ prompt, context?, maxTokens? }) → { text, backend }` types and fake-backed unit tests exist
- [ ] Explicit: no JSON/schema/tool fields on the public request/response
- [ ] Refuse-when-not-installed behavior is specified in tests (fake status)
