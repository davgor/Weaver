# 032.2 — Create / query inventory APIs

APIs to create items into a character inventory and query held/equipped state.

**Parent:** `032-ItemEngine-Item-Model-And-Inventory`. **Depends on:** `032.1`.

## Acceptance criteria

- [ ] Create + query APIs are unit-tested for a character inventory
- [ ] Query distinguishes template facts vs instance state
- [ ] Admin-callable endpoints registered for the new APIs
