# 098.1 — Scaffold CampaignEngine package + registry wiring

Create `packages/CampaignEngine` (`@weaver/campaign-engine`) stub with health/catalog surface. Wire `build:engines`, ElectronAdmin, AITTRPG `REQUIRED_ENGINE_IDS`, root README, delivery-standards lists.

**Parent:** `098-CampaignEngine-Full-Bootstrap-And-Admin-Inspect`.

## Acceptance criteria

- [ ] Package builds; Vitest covers health / listEndpoints / call / unknown endpoint
- [ ] Registered in Admin + AITTRPG catalogs and health summary
- [ ] Root README documents CampaignEngine as full-campaign bootstrap + inspect culmination (LLM-free structure; Narration for prose stages)
