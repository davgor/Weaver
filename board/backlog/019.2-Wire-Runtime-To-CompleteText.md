# 019.2 — Wire createLlmEngine / runtime to completeText

Connect the real `node-llama` runtime path to `completeText`, mapping prompt+context → model input without JSON/tool modes.

**Parent:** `019-LLMEngine-Text-Passthrough-API`. **Depends on:** `019.1`.

## Acceptance criteria

- [ ] Default engine singleton exposes `completeText` backed by the existing runtime
- [ ] Prompt + optional context are concatenated/mapped without structured-output modes
- [ ] Unit tests cover happy path with injectable runtime; installed-gate still enforced
