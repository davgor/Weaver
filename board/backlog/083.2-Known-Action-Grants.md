# 083.2 — Known-action grant/revoke/query

Track which `actionId`s a character knows without duplicating action definitions.

**Parent:** `083-ActionEngine-Catalog-And-Known-Actions`. **Depends on:** `083.1`.

## Acceptance criteria

- [ ] Grant/revoke/query APIs keyed by character id + action id, unit-tested
- [ ] Granting an unknown catalog id fails closed
- [ ] Definitions remain only in the ActionEngine catalog (no copied range/effect blobs on the character)
