# 084.3 — Action-turn lockout + CombatEngine contract

Catalog turn cost locks the caster's Action slot via CombatEngine encounter state.

**Parent:** `084-ActionEngine-Use-Resolution-And-Lockout`. **Depends on:** `084.1`, `048-CombatEngine-Encounter-Lifecycle`.

## Acceptance criteria

- [ ] Successful use requests/applies lockout for catalog turn count
- [ ] `*.contract.test.ts` exercises CombatEngine's real published turn/Action API
- [ ] No mana pool; second Action in lockout window fails closed
