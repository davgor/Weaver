# 082.1 — Scaffold ActionEngine package + registry wiring

Create `packages/ActionEngine` (`@weaver/action-engine`) as a deterministic engine stub with health/catalog surface matching siblings. Wire into `build:engines`, ElectronAdmin `engines` array, and AITTRPG `REQUIRED_ENGINE_IDS`. No ability/effect APIs yet.

**Parent:** `082-ActionEngine-Ability-Effect-And-Range-Model`.

## Acceptance criteria

- [ ] Package exists with tsc build, Vitest health/listEndpoints/call/unknown-endpoint coverage
- [ ] Root `build:engines` includes `@weaver/action-engine`
- [ ] ElectronAdmin and ElectronAITTRPG register the package (Admin engines array + `REQUIRED_ENGINE_IDS`)
- [ ] Root README package table lists ActionEngine (no longer "planned-only" once scaffold lands)
