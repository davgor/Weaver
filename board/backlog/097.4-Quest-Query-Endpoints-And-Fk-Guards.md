# 097.4 — List/get APIs, endpoints, dangling-FK guards

Query surface + admin endpoints; reject definitions/seeds that reference missing peers when lookups are provided.

**Parent:** `097-QuestEngine-World-Quest-Seeding`. **Depends on:** `097.3`.

## Acceptance criteria

- [ ] `listWorldQuests` / `getWorldQuest` (and delete/clear-campaign if needed) unit-tested
- [ ] Admin `call` endpoints cover seed/list/get
- [ ] Optional injected `QuestReferenceLookup` rejects unknown NPC/place/item ids at seed/define time with typed errors
- [ ] CharacterEngine quest log is not written by QuestEngine in this ticket
