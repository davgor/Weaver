# 012.6 — Supporting APIs (GetCell, bounds/meta, expansion getters, list/delete/has)

Implement the supporting query/lifecycle surface listed on the parent epic.

**Parent:** `012-WorldEngine-Chunked-Map-Store`. **Depends on:** `012.3`, `012.4`.

## Acceptance criteria

- [ ] `GetCell`, `GetWorldBounds`, `GetWorldMeta`, `GetExpansion`, `ListExpansions`, `GetLatestExpansion` behave per epic table and are unit-tested
- [ ] `ListWorlds`, `DeleteWorld`, `HasWorld` cover discovery/teardown/existence
- [ ] Admin `listEndpoints` / `call` expose the new methods (payload via 078 plumbing)
