# 094.5 — WeatherEngine→WorldEngine contract tests + package README

Pin the peer boundary: WeatherEngine applies weather through WorldEngine’s published overlay/cell APIs (real provider, no mocks of WorldEngine public API). Document the package.

**Parent:** `094-WeatherEngine-Climate-And-World-Mutations`. **Depends on:** `094.4`.

## Acceptance criteria

- [ ] `*.contract.test.ts` in WeatherEngine exercises real `@weaver/world-engine`: apply → mutated getCell → clear → restored
- [ ] Package README documents role, mutation model, public APIs, LLM-free boundary
- [ ] WorldEngine README documents sparse-overlay APIs and `landTypeOverride` effective reads
