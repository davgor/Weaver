# 094.1 — Scaffold WeatherEngine package + registry wiring

Create `packages/WeatherEngine` (`@weaver/weather-engine`) as a deterministic engine stub with health/catalog surface matching siblings. Wire into `build:engines`, ElectronAdmin `engines` array, AITTRPG `REQUIRED_ENGINE_IDS`, root README, and delivery-standards engine lists. No weather APIs yet (those are later `094.*` tickets).

**Parent:** `094-WeatherEngine-Climate-And-World-Mutations`.

## Acceptance criteria

- [ ] `packages/WeatherEngine` exists with `@weaver/weather-engine`, `tsc` build, and a `health` endpoint matching sibling engine stubs
- [ ] Vitest covers health / listEndpoints / call / unknown-endpoint rejection
- [ ] Root `build:engines` includes `@weaver/weather-engine`
- [ ] ElectronAdmin and ElectronAITTRPG depend on the package and register it in their engine catalogs
- [ ] AITTRPG `REQUIRED_ENGINE_IDS` / `summarizeEngineHealth` includes `WeatherEngine` (tests updated)
- [ ] Root README package table documents WeatherEngine’s role (climate fields that mutate WorldEngine results; LLM-free)
- [ ] Delivery-standards skill engine lists include `WeatherEngine`
