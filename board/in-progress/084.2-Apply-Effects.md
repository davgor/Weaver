# 084.2 — Apply action effects to targets

Apply the action's `effects[]` (at least `slow_movement`) as structured outcomes.

**Parent:** `084-ActionEngine-Use-Resolution-And-Lockout`. **Depends on:** `084.1`.

## Acceptance criteria

- [ ] Successful use returns/applies effect payloads for each effect on the action
- [ ] Ice Bolt and Hamstring Strike both yield `slow_movement` in tests
- [ ] Effect magnitudes come from catalog/effect defs, not caller free text
