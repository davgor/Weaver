# 013.5 — Query APIs (GetRegion, GetRegionAt, summaries) + TDD

Supporting query surface for consumers (CivilizationEngine, DM grounding).

**Parent:** `013-RegionalEngine-Map-Segmentation`. **Depends on:** `013.4`.

## Acceptance criteria

- [ ] `GetRegion`, `GetRegionAt`, and summary helpers are unit-tested
- [ ] LLM-ready core summary fields are present without display names/prose
- [ ] Admin endpoints expose the query APIs
