/** User-visible product name; must match `package.json` → `build.productName`. */
export const APP_DISPLAY_NAME = 'AI Visual Novel'

/** Boot loading-screen brand line (epic 120). */
export const BOOT_BRAND_TITLE = 'AI Visual Novel powered by Weaver'

/** Windows executable basename; must match `package.json` → `build.executableName`. */
export const APP_EXE_NAME = 'AI-Visual-Novel.exe'
